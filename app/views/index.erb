<script>
  window.fbAsyncInit = function() {
    FB.init({
      appId      : 1585228831711791,
      xfbml      : true,
      version    : 'v2.1'
    });
  };

  (function(d, s, id){
     var js, fjs = d.getElementsByTagName(s)[0];
     if (d.getElementById(id)) {return;}
     js = d.createElement(s); js.id = id;
     js.src = "//connect.facebook.net/en_US/sdk.js";
     fjs.parentNode.insertBefore(js, fjs);
   }(document, 'script', 'facebook-jssdk'));

  function checkLoginState() {
	  FB.getLoginStatus(function(response) {
      if (response.status === 'connected') {
        redirectToSignUp();
      } else {
        FB.login(function(response) {
          if (response.status === 'connected') {
            redirectToSignUp();
          }
        });
      }
    });
  };

  function redirectToSignUp() {
  	FB.api('/me', function(response) {
      $.ajax({
        url: "/fb/api/users",
        dataType: "JSON",
        type: "post",
        data: response
      }).done(function(response){
        if (response.email === undefined) {
          window.location.replace(response);
        } else {
          window.location.replace("/sessions/new?notice=" + response.notice + "&email=" + response.email);
        };
      }).fail(function(response){
        window.location.replace("/?notice=Something%20went%20wrong%20with%20facebook");
      });
  	});
  };
</script>
<div id="background-image"></div>

<div class="landing-page-content">
  <a href="/"><img src="images/logo-2.png" class="rotate" alt="circle-logo" id="logo" title="Born through DBC!"></a>
  <h1>circle</h1>
  <% if !current_user %>
    <div class="login-links" align="center"><a href="/sessions/new" id="login">see your family</a></div>
    <div class="login-links" align="center"><a href="/users/new" id="signup">create a circle</a></div>
  <% end %>
    <div class="login-links" align="center"><a href="#" onclick="checkLoginState();" id="fb-login">login with facebook</a></div>
</div>
<div class="landing-page-slogan">
  <h1>welcome home.</h1>
</div>
<div id="fb-root"></div>  


