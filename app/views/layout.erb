<!DOCTYPE html>
<html lang="en">
<head>

  <link rel="stylesheet" href="/css/normalize.css">
  <link rel="stylesheet" href="/css/application.css">
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Circle | A Digital Living Room for your Family</title>
</head>
<body>
  <nav>
    <% if current_user && !current_family.nil? %>
      <form action="/tags/search" method="get">
        <input type="text" name="tags" placeholder="search for tags..separate by comma">
        <input type="submit" value="search">
      </form>
      <a href="/families/<%= current_family.id %>/show">home</a> |
      <a href="/families/<%= current_family.id %>/posts/new">new post</a> |
      <a href="/families/<%= current_family.id %>/polls/new">new poll</a> |
      <a href="/families/<%= current_family.id %>/albums/new">new album</a> |
      <a href="/families/<%= current_family.id %>/photos/new">new photo</a> |
      <a href="/users/<%= current_user.id %>/edit">account settings</a>
      <% unless current_user.family.nil? %>
        <% if current_user.admin %>
          | <a href="/families/<%= current_family.id %>/edit">Family Settings</a>
        <% end %>
      <% end %>
      | <a href="/families/<%= current_family.id %>/users/<%= current_user.id %>/inbox">Inbox</a>
      | <form action="/sessions/destroy" method="post">
        <input name="_method" type="hidden" value="delete" />
        <input type="submit" value="Sign Out">
      </form>
    <% else %>
      <a href="/">home</a>
    <% end %>
  </nav>
  <div class="container">
    <div class="notice"><p><%= @notice %></p></div>
    <%= yield %>
  </div>
</body>
</html>
