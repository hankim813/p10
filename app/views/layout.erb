<!DOCTYPE html>
<html lang="en">
<head>

  <link rel="stylesheet" href="/css/normalize.css">
  <link rel="stylesheet" href="/css/application.css">
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title>Circle | A Digital Living Room for your Family</title>
</head>
<body>
  <nav>
    <% if current_user %>
      <form id="sign-out" action="/sessions/destroy" method="post">
        <input name="_method" type="hidden" value="delete" />
        <input type="submit" value="Sign Out">
      </form>
      <a href="/users/<%= current_user.id %>/edit">Account Settings</a>
      <% if current_user.admin %>
        <a href="/families/<%= current_user.family.id %>/edit">Family Settings</a>
      <% end %>
    <% end %>
  </nav>
  <%= yield %>
</body>
</html>
